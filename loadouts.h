#ifndef LOADOUTS_H
#define LOADOUTS_H

#include <QMainWindow>

class loadouts : public QMainWindow
{
    Q_OBJECT
public:
    explicit loadouts(QWidget *parent = nullptr);

signals:
};

#endif // LOADOUTS_H
